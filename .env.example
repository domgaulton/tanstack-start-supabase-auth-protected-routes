# Supabase (required)
# For local development, run `npx supabase status` and use the values printed.
# Create a .env.local file with these values (it overrides .env and is gitignored).

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<ANON_KEY from supabase status>
SUPABASE_SECRET_KEY=<SECRET_KEY from supabase status>

# Vercel (optional — for deployment)
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=

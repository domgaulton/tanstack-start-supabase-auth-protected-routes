# Supabase credentials
#
# Both .env and .env.local are gitignored. Use them as follows:
#
#   .env        — Hosted/production Supabase credentials
#   .env.local  — Local development overrides (takes precedence over .env)
#
# For local dev, run `npx supabase status` and use the values printed.
# For hosted Supabase, find these in your project settings under API.

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<ANON_KEY from supabase status>
SUPABASE_SECRET_KEY=<SECRET_KEY from supabase status>

# Vercel (optional — for deployment)
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=

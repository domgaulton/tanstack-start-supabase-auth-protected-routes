# Supabase credentials
#
# Both .env and .env.local are gitignored. Use them as follows:
#
#   .env        — Hosted/production Supabase credentials
#   .env.local  — Local development overrides (takes precedence over .env)
#
# Quick start:
#   cp .env.example .env && cp .env.example .env.local
#
# ──────────────────────────────────────────────────────────────────────────────
# LOCAL DEVELOPMENT
#
#   1. Start local Supabase:  npm run db:start
#   2. Get your keys:         npx supabase status
#   3. Paste the "API URL", "anon key", and "service_role key" below
#
# ──────────────────────────────────────────────────────────────────────────────
# HOSTED SUPABASE
#
#   1. Create a project:      https://database.new
#   2. Find your keys:        https://supabase.com/dashboard/project/_/settings/api
#   3. Paste the "Project URL", "anon public" key, and "service_role" key below
#
# ──────────────────────────────────────────────────────────────────────────────

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
SUPABASE_SECRET_KEY=
# OPTIONAL_SUPABASE_POSTGRES_PASSWORD=

# Vercel (optional — for deployment)
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=

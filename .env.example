# Supabase credentials
#
# Both .env and .env.local are gitignored. Use them as follows:
#
#   .env        — Hosted/production Supabase credentials
#   .env.local  — Local development overrides (takes precedence over .env)
#
# Quick start:
#   cp .env.example .env && cp .env.local.example .env.local
#
# ──────────────────────────────────────────────────────────────────────────────
# HOSTED SUPABASE
#
#   1. Create a project:      https://database.new
#   2. Find your keys:        https://supabase.com/dashboard/project/_/settings/api
#   3. Replace the URL below and uncomment the keys
#
# ──────────────────────────────────────────────────────────────────────────────

# Default local URL — replace with your hosted Supabase project URL
VITE_SUPABASE_URL=http://127.0.0.1:54321

# Uncomment and fill in with your hosted Supabase keys:
#VITE_SUPABASE_ANON_KEY=
#SUPABASE_SECRET_KEY=

# Vercel (optional — for deployment)
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=
